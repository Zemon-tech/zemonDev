# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# API Configuration
VITE_BACKEND_URL=https://your-backend.onrender.com

# Note: VITE_API_BASE_URL is no longer needed as it's derived from VITE_BACKEND_URL
# All API calls will use ${VITE_BACKEND_URL}/api

# Landing Site URL (for redirects)
VITE_LANDING_URL=https://your-landing-site.vercel.app

# Environment
VITE_NODE_ENV=production
